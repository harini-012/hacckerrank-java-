import java.util.*;
class ProductNode
{
    int proId;
    ProductNode next;
    public ProductNode(int proId)
    {
        this.proId=proId;
        this.next=null;
    }
}
public class Solution
{
    ProductNode head;
    public void insertAtEnd(int proId)
    {
        ProductNode newNode=new ProductNode(proId);
        if (head==null)
        {
            head=newNode;
        }
        else
        {
            ProductNode temp=head;
            while(temp.next!=null)
            {
                temp=temp.next;
                
            }
            temp.next=newNode;
        }
    }
    public void display()
    {
        if(head==null)
        {
            System.out.println("No product id in the cart");
            return;
        }
        ProductNode temp=head;
        while(temp!=null)
        {
            System.out.print(temp.proId+" ");
            temp=temp.next;
        }
    }
    public static void main(String[]args)
    {
        Solution pi=new Solution();
        Scanner sc=new Scanner(System.in);
        int n=sc.nextInt();
        for(int i=0;i<n;i++)
        {
            int proId=sc.nextInt();
            pi.insertAtEnd(proId);
        }
        pi.display();
    }
}
